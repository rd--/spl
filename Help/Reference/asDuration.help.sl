# asDuration

- _asDuration(anObject)_

At `Frequency`,
answer the reciprocal:

```
>>> Frequency(100).asDuration
Duration(0.01)
```

The inverse is `asFrequency`:

```
>>> Duration(0.01).asFrequency
Frequency(100)
```

* * *

See also: asFrequency, Duration, Frequency
