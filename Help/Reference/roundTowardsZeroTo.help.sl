# roundTowardsZeroTo

- _roundTowardsZeroTo(n, m)_

Answer the nearest number to _n_ that is a multiple of _m_, rounding towards zero.

```
>>> 0.9.roundTowardsZeroTo(1)
0

>>> -0.9.roundTowardsZeroTo(1)
0
```

* * *

See also: round, roundTo, roundTowardsZero
