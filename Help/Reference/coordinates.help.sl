# coordinates

- _coordinates(x)_

Answer a `List` of the coordinates of the object _x_.

At `BarycentricCoordinates`:

```
>>> BarycentricCoordinates([1 3 5])
>>> .coordinates
[1 3 5]
```

At `CartesianCoordinates`:

```
>>> CartesianCoordinates([1 3 5])
>>> .coordinates
[1 3 5]
```

At `CylindricalCoordinates`:

```
>>> CylindricalCoordinates([1 3 5])
>>> .coordinates
[1 3 5]
```

At `GeodeticCoordinates`:

```
>>> GeodeticCoordinates([1 3 5])
>>> .coordinates
[1 3 5]
```

At `HexagonalCoordinates`:

```
>>> HexagonalCoordinates([1 3 5])
>>> .coordinates
[1 3 5]
```

At `HexagonalEfficientCoordinates`:

```
>>> HexagonalEfficientCoordinates([1 3 5])
>>> .coordinates
[1 3 5]
```

At `PlanarCoordinates`:

```
>>> PlanarCoordinates([1 3])
>>> .coordinates
[1 3]
```

At `PolarCoordinates`:

```
>>> PolarCoordinates([1 3])
>>> .coordinates
[1 3]
```

At `SphericalCoordinates`:

```
>>> SphericalCoordinates([1 3 5])
>>> .coordinates
[1 3 5]
```

At `List`:

```
>>> [1 3 5].coordinates
[1 3 5]
```

* * *

See also: BarycentricCoordinates, CartesianCoordinates, CylindricalCoordinates, GeodeticCoordinates, HexagonalCoordinates, HexagonalEfficientCoordinates, PlanarCoordinates, PolarCoordinates, SphericalCoordinates

Guides: Coordinate System Functions, Geometry Functions
