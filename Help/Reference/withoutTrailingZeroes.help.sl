# withoutTrailingZeroes

- _withoutTrailingZeroes([x₁ x₂ …])_

Answer a `List` that is like the list _x_ but without any trailing zeroes.

Remove all trailing zeroes:

```
>>> [1 1 0 0 0].withoutTrailingZeroes
[1 1]
```

A leading zero is not a trailing zero:

```
>>> [0 0 0].withoutTrailingZeroes
[0]
```

* * *

See also: isZero

Guides: List Functions

References:
_W_
[1](https://en.wikipedia.org/wiki/Trailing_zero)
