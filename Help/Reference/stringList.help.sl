# stringList

- _stringList(aString)_

Answer a `List` of `String`s each holding one character of _aString_.

```
>>> 'text'.stringList
['t' 'e' 'x' 't']
```

The `elementType` of the answer is `String`:

```
>>> 'text'.stringList.elementType
'String'
```

`contents` is another, and briefer, name for this behaviour:

```
>>> 'text'.contents
['t' 'e' 'x' 't']
```

`characterList` answers a list of `Character` values:

```
>>> 'text'.characterList.elementType
'Character'
```

* * *

See also: characterList, contents, String
