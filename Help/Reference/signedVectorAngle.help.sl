# signedVectorAngle

- _signedVectorAngle(u, v)_

Answers the signed angle between the vectors _u_ and _v_.
The answer is in _(-π,π)_.

The signed angle between two vectors in two-dimensions:

```
>>> [1 0].signedVectorAngle([1 1])
1/4.pi

>>> [1 1].signedVectorAngle([1 0])
-1/4.pi
```

* * *

See also: atan2, counterClockwiseVectorAngle, vectorAngle

Guides: Geometry Functions
