# On Integer Sequences

## 0,1 sequences

SplFragment(thueMorseSequence,svg=A)

SplFragment(divisible,svg=J)

SplFragment(divisible,svg=K)

SplFragment(isSquareFree,svg=F)

SplFragment(padLeft,svg=D)

SplFragment(partitionFunctionP,svg=D)

SplFragment(squaresR,svg=B)

<bitLength,D

<pascalTriangle,D

<padRight,B

<binaryExpansion,M

<triangularArray,K

<isPerfectSquare,B

<isInteger,B

## _-1,1_ sequences

SplFragment(liouvilleLambda,svg=A)

SplFragment(rudinShapiro,svg=C)

SplFragment(repeat,svg=C)

SplFragment(repeat,svg=A)

SplFragment(radical,svg=C)

SplFragment(thueMorseSequence,svg=E)

## _-1,0,1_ sequences

SplFragment(cyclotomic,svg=H)

SplFragment(thueMorseSequence,svg=B)

SplFragment(jacobiSymbol,svg=D)

SplFragment(jacobiSymbol,svg=E)

SplFragment(kroneckerSymbol,svg=D)

SplFragment(kroneckerSymbol,svg=I)

<moebiusMu,L
