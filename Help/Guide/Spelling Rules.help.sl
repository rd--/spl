# Spelling Rules

Prefer `center` to centre, also `centered` to centred.

Prefer `colour` to color.

Prefer _generalised_ to generalized.

Prefer _grey_ to gray when a colour, Gray codes are named for a person.

Prefer `zeroes` to zeros.
