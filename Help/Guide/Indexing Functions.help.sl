# Indexing Functions

Access an element at a specified index:

- `at`
- `atIfAbsent`
- `atIfPresent`
- `atIfPresentIfAbsent`

Access an element at a specified index or at a modified index if the specified index is invalid:

- `atFold`
- `atMod`
- `atPin`
- `atSymmetrical`
- `atWrap`

Access a set of element at the specified indices:

- `atAll`
- `atAllPin`
- `atAllSymmetrical`

Access an element using a positional index:

- `atPath`

Write a value to a slot at a specified index:

- `atIfAbsentPut`
- `atPut`

Write a value, or a collection of values, to a specified sequence of indices:

- `atAllPut`
- `atAllPutAll`
