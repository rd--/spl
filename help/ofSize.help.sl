# ofSize -- instance creation

_ofSize(anInteger)_

Create a new collection of size _anInteger_ with nil as its elements.
This method exists because for some collection types the _new(anInteger)_ constructor may create an empty collection.
