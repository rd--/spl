# Set

- add(self, newObject): Include newObject as one of the receiver's elements, but only if not already present. Answer newObject.
- ifAbsentAdd(self, anObject): Ensure anObject is part of the receiver.  Answer whether its membership was newly acquired.
