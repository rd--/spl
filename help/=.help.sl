# = ≡ equals - equality operator

- _p = q_ ≡ _equals(p, q)_

This is the _structural equality_ operator, it answers _true_ if _p_ is equal to _q_, else _false_.
The meaning of _equals_ is determined by the _typeOf(p)_.
The _equals_ method is implemented as identity at _Object_.

The _=_ symbol is also a part of the [Initialised Temporaries Syntax].

* * *

See also _==_.
