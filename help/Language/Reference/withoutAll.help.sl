# withoutAll -- removing

- _without(aCollection, anotherCollection)_

Remove every element of _aCollection_ that compares equal to an element of _anotherCollection_, and answer _aCollection_.

	| c = [1 2 2 3 3 3]; | c.withoutAll([1, 3]) = [2 2]

* * *

See also: without
