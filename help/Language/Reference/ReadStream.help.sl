# ReadStream -- collection type

A _ReadStream_ is an accessor for a sequence of objects that can only read objects from the sequence.

The basic protocols on a ReadStream are _next_ and _reset_.

* * *

See also: next, peek, reset, Stream, PositionableStream, WriteStream
