# subStrings

- _subStrings(aString, separators)_

Answer a `List` containing the substrings in _aString_ separated by the elements of _separators_.

```
>>> 'abbccba'.subStrings('ab')
['cc']

>>> 'a word, another'.subStrings(' ,')
['a' 'word' 'another']
```

* * *

See also: splitByRegExp

References:
_Smalltalk_
5.7.10.15
