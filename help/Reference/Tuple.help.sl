# Tuple

An n-tuple is an ordered set of n elements.

Each n-tuple is a distinct type, _TwoTuple_, _ThreeTuple_ &etc.

The _Tuple_ method is defined at arities two through four.

There is a syntax for writing tuples, _(p, q) ⇒ Tuple(p, q)_ &etc.

* * *

See also: List

Categories: Collection, Trait
