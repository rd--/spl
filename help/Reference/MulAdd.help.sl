# MulAdd

- _MulAdd(input, mul=1, add=0)_

Multiply and add.
Equivalent to _input * mul + add_ but potentially more efficient.

At `SmallFloat`:

```
>>> 3.MulAdd(3, -2)
3 * 3 - 2
```

* * *

See also: +, *
