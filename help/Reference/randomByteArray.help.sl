# randomByteArray

_randomByteArray(anInteger)_

Answer a ByteArray of _anInteger_ places, each randomly initialized.

```
>>> let bytes = 16.randomByteArray;
>>> (bytes.isByteArray, bytes.size, bytes.hex.size)
(true, 16, 32)
```

* * *

See also: randomInteger
