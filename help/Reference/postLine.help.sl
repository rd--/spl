# postLine

- _postLine(aSystem, aString)_
- _postLine(aString)_ ⟹ _postLine(system, aString)_

Print a string to the console, followed by a new line.

If a `Transcript` is loaded, the message will also be written to the transcript,
where it may be viewed using a `TranscriptViewer`.

* * *

See also: Transcript

Categories: Printing
