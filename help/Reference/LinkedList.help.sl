# LinkedList -- collection type

A collection of links, which are containers for other objects.

Using the methods _addFirst_ and _removeLast_ causes the list to behave as a stack.

Using _addLast_ and _removeFirst_ causes the list to behave as a queue.

Unlike Array certain methods, such as atIndex, are linear and not constant time.
