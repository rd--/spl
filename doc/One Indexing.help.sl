# One Indexing

Arrays and other seqenceable collections are one-indexed, as in Fortran, Apl, Algol68, Smalltalk, Icon, Excel, Mathematica, Lua, R, Matlab, Julia.

The first element is at index one, _at(c, 1)_ reads the first element of the the collection _c_.

The index of the last element of an array is the size of the array.
