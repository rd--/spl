```
let testCount = 0;
let passCount = 0;
let errorCount = 0;
system
.splFile('Program/SuperCollider/Texture Collection')
.readDirectoryFileNames
.do { :fileName |
	let fileText = fileName.readTextFile;
	let programTexts = fileText.paragraphs.takeWhile { :each |
		each.beginsWith('#').not
	};
	[fileName, programTexts.size].postLine;
	programTexts.do { :each |
		let result = system.evaluate(each);
		testCount := testCount + 1;
		result.isTextureProgram.if { /* isOutputSignal */
			passCount := passCount + 1
		} {
			['FAIL', each].postLine
		}
	}
};
[testCount, passCount, errorCount].postLine
```

Graph => 391
Graph Collection => 349
Ugen => 503
Texure => 70
Texure Collection => 17
